# Local development settings

AWS_REGION=us-west-2
BEDROCK_DEFAULT_MODEL=amazon.nova-lite-v1:0

# Optional local backend config
APP_ENV=local
CORS_ORIGINS=http://localhost:5173

# Do not put long-lived AWS credentials here if using normal AWS CLI profiles.
# Prefer: aws configure sso / aws configure / environment credentials / IAM roles.
